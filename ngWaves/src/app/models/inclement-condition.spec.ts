import { InclementCondition } from './inclement-condition';

describe('InclementCondition', () => {
  it('should create an instance', () => {
    expect(new InclementCondition()).toBeTruthy();
  });
});
